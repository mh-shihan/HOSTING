# Create Subdomain on Hosting Provider

Go to the DNS management section of your hosting provider's control panel.

```bash
type -> A
name -> api
name -> www
name -> www.api
content -> <Your VPS IP Address>
TTL -> Automatic
```
